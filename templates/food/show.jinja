{% extends 'base.jinja' %} {% block content %}

<div class="container">
    <ul class="food-list">
        <li class="food-list__items">
            {% for food in foods %}
                <a href="{{ food.id }}/edit">{{ food.name }}: {{ food.category }}</a>             
            {% endfor %}
        </li>
    </ul>Main
        <li>{{ main.name }}</li>
    </ul>
    <ul>Fruit
        <li>{{ fruit.name }}</li>
    </ul>

    <ul>
        <li>
        </li>
    </ul>
</div>
    
{% endblock content %}